import * as React from 'react';
import { Card, Title, Paragraph } from 'react-native-paper';

const Textbox = () => (
  <Card>
    <Card.Content>
      <Paragraph>lets just see how much it extends just to test it out. it seems like the more text, the more it'll extend vertically!</Paragraph>
    </Card.Content>
  </Card>
);

export default Textbox;

